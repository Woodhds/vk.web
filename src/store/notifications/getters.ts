import type { GetterTree } from 'vuex'
import type { NotificationState } from '~/store/notifications/types'
import type { RootState } from '~/store/types'

const getters = {

} as GetterTree<NotificationState, RootState>

export default getters
